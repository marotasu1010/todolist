%p#notice= notice

%p
  %b タスク名:
  = @task.name
%p
  %b タスク:
  = @task.status
%p
  %b メモ:
  = @task.content

= link_to 'Edit', edit_task_path(@task), class: 'btn btn-success'
= link_to 'home', root_url, class: 'btn btn-warning'
